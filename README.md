
  # أداة تحديد نوع الأحجار

  This is a code bundle for أداة تحديد نوع الأحجار. 
  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  
